/**
 * capital-growth — browser entry point.
 *
 * Just importing CapitalGrowthController is enough:
 * it registers DOMContentLoaded and exposes all globals.
 */
import './ui/CapitalGrowthController.js';
